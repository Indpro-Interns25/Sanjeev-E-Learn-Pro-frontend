// Deprecated wrapper - re-export the canonical VideoPlayer component
// This file used to contain an older copy of VideoPlayer. To avoid duplication
// we now re-export the main `VideoPlayer.jsx` implementation. The file can be
// safely deleted once repository tooling allows removing files.

import VideoPlayer from './VideoPlayer';
export default VideoPlayer;